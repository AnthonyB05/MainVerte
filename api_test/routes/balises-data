const express = require('express')
const router = express.Router();
const balise_data = require('../../balises-data.json')

router.get('/', (req,res,next) => {    res.status(200).json(balise_data)})

module.exports = router;