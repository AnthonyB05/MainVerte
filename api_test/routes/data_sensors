const express = require('express')
const router = express.Router();
const data_sensors = require('../../data_sensors.json')

router.get('/', (req,res,next) => {    res.status(200).json(data_sensors)})

module.exports = router;