const express = require('express')
const router = express.Router();
const sensors = require('../../sensors.json')

router.get('/', (req,res,next) => {    res.status(200).json(sensors)})

module.exports = router;